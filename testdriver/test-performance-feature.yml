version: 4.0.79
steps:
  - prompt: Click on "Performance" from the sidebar.
    commands:
      - command: hover-text
        text: Performance
        description: Performance option in the sidebar
        action: click
  - prompt: Assert the feature's details or dashboard loads.
    commands:
      - command: assert
        expect: the Performance Dashboard is visible
  - prompt: >-
      Interact with elements within the feature to ensure they're functional.  #
      Use the content of the file from the environment
    commands:
      - command: hover-text
        text: View Current Bets
        description: button to view current bets
        action: click
